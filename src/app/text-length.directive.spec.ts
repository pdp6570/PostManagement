import { TextLengthDirective } from './text-length.directive';

describe('TextLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new TextLengthDirective();
    expect(directive).toBeTruthy();
  });
});
